# SQL 기초

SQL(Structured Query Language)은 데이터베이스에서 데이터를 조회, 추가, 수정, 삭제하는 언어입니다.

## 📌 목표

- SELECT 문으로 데이터 조회하기
- WHERE 절로 조건에 맞는 데이터 필터링하기
- JOIN으로 여러 테이블의 데이터 연결하기
- GROUP BY와 집계함수로 통계 산출하기

## 📚 학습 순서

1. **[SELECT 기초](./01-SELECT-기초.md)** ⭐ 시작
   - 기본 조회 문법
   - 특정 컬럼 선택
   - 별칭(Alias) 사용
   - LIMIT으로 개수 제한

2. **[WHERE 조건](./02-WHERE-조건.md)**
   - 조건부 조회
   - 비교 연산자 (=, >, <, >=, <=, !=)
   - 논리 연산자 (AND, OR, NOT)
   - LIKE, IN, BETWEEN 활용

3. **[JOIN 활용](./03-JOIN-활용.md)**
   - INNER JOIN
   - LEFT JOIN
   - 여러 테이블 조인하기

4. **[집계함수](./04-집계함수.md)**
   - COUNT, SUM, AVG, MIN, MAX
   - GROUP BY로 그룹핑
   - HAVING으로 그룹 필터링

5. **[실습 예제](./05-실습-예제.md)**
   - 종합 실습 문제
   - 실무 시나리오 연습

## ⏱️ 예상 소요 시간

- 각 주제당 30-60분
- 실습 포함 **총 4-6시간**

## 💡 학습 팁

### 효과적인 SQL 학습법

1. **직접 타이핑하기**
   ```sql
   -- 나쁜 예: 복사-붙여넣기만
   -- 좋은 예: 직접 타이핑하며 이해하기
   ```

2. **쿼리 실험하기**
   - 예제를 조금씩 변형해보기
   - 결과가 어떻게 바뀌는지 관찰하기

3. **AI와 함께 학습하기**
   ```
   좋은 질문: "employees 테이블에서 급여가 5000 이상인 직원을
               조회하는 SQL을 작성하고, 각 부분이 무슨 의미인지
               설명해줘"

   나쁜 질문: "SQL 가르쳐줘"
   ```

4. **오류를 두려워하지 않기**
   - 오류 메시지를 읽고 이해하기
   - 오류를 통해 배우기

## 📖 SQL 기본 문법 미리보기

### 기본 구조

```sql
SELECT 컬럼1, 컬럼2         -- 어떤 컬럼을 가져올지
FROM 테이블명              -- 어느 테이블에서
WHERE 조건                -- 어떤 조건으로
GROUP BY 그룹컬럼          -- 어떻게 묶을지
HAVING 그룹조건           -- 묶인 결과의 조건
ORDER BY 정렬컬럼          -- 어떻게 정렬할지
LIMIT 개수;               -- 몇 개나 가져올지
```

### 실행 순서 (중요!)

SQL은 작성 순서와 실행 순서가 다릅니다:

```
작성 순서:  SELECT → FROM → WHERE → GROUP BY → HAVING → ORDER BY
실행 순서:  FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY
```

## ✅ 학습 후 체크리스트

이 섹션을 완료하면 다음을 할 수 있어야 합니다:

- [ ] SELECT로 테이블의 모든 데이터 조회
- [ ] 특정 컬럼만 선택하여 조회
- [ ] WHERE로 조건에 맞는 데이터 필터링
- [ ] AND, OR로 복잡한 조건 작성
- [ ] INNER JOIN으로 두 테이블 연결
- [ ] LEFT JOIN의 차이점 이해
- [ ] COUNT, SUM 등 집계함수 사용
- [ ] GROUP BY로 그룹별 통계 산출

## 🚀 시작하기

준비되셨나요? 그럼 첫 번째 주제부터 시작해봅시다!

👉 **[SELECT 기초로 이동](./01-SELECT-기초.md)**

---

## 참고 자료

- [SQL Tutorial - W3Schools](https://www.w3schools.com/sql/)
- [PostgreSQL Documentation](https://www.postgresql.org/docs/)
- [MySQL Documentation](https://dev.mysql.com/doc/)
