# DBeaver 설치 가이드

DBeaver는 무료로 사용할 수 있는 데이터베이스 관리 도구입니다. MySQL, PostgreSQL, Oracle 등 다양한 데이터베이스를 지원합니다.

## 1. DBeaver 다운로드

### 공식 웹사이트 접속

1. 브라우저에서 https://dbeaver.io/download/ 접속
2. **Community Edition** (무료 버전) 선택
3. 운영체제에 맞는 설치 파일 다운로드
   - Windows: `.exe` 파일
   - macOS: `.dmg` 파일

> 💡 **Tip**: Enterprise Edition이 아닌 **Community Edition**을 선택하세요. 우리가 필요한 모든 기능이 포함되어 있습니다.

## 2. 설치하기

### Windows

1. 다운로드한 `.exe` 파일 실행
2. 설치 마법사 지시에 따라 진행
   - "Next" 클릭
   - 설치 경로 선택 (기본값 권장)
   - "Install" 클릭
3. 설치 완료 후 "Finish" 클릭

### macOS

1. 다운로드한 `.dmg` 파일 열기
2. DBeaver 아이콘을 Applications 폴더로 드래그
3. Applications 폴더에서 DBeaver 실행
4. "확인되지 않은 개발자" 경고가 나타나면:
   - 시스템 환경설정 > 보안 및 개인정보 보호
   - "확인 없이 열기" 클릭

## 3. 첫 실행 및 초기 설정

### 3.1. DBeaver 실행

- Windows: 시작 메뉴에서 "DBeaver" 검색 후 실행
- macOS: Applications 폴더에서 DBeaver 실행

### 3.2. 샘플 데이터베이스 생성 (선택)

처음 실행 시 샘플 데이터베이스 생성을 물어볼 수 있습니다:
- 연습용으로 "Yes" 선택 권장
- 나중에 언제든지 추가 가능

### 3.3. 인터페이스 둘러보기

DBeaver 화면 구성:

```
┌─────────────────────────────────────────────────────┐
│  File  Edit  View  Database  ...          [도구막대]│
├──────────┬────────────────────────────────┬─────────┤
│          │                                │         │
│ Database │                                │ Project │
│ Navigator│      SQL Editor                │ Explorer│
│          │      (SQL 쿼리 작성 영역)        │         │
│          │                                │         │
│          ├────────────────────────────────┤         │
│          │     Results                    │         │
│          │     (쿼리 실행 결과 표시)        │         │
├──────────┴────────────────────────────────┴─────────┤
│  Status Bar                                         │
└─────────────────────────────────────────────────────┘
```

주요 패널:
- **Database Navigator** (왼쪽): 연결된 데이터베이스 목록
- **SQL Editor** (중앙 상단): SQL 쿼리 작성
- **Results** (중앙 하단): 쿼리 실행 결과 표시
- **Project Explorer** (오른쪽): 프로젝트 파일 관리

## 4. 데이터베이스 연결하기

### 4.1. 새 연결 생성

1. 상단 도구막대에서 "New Database Connection" 버튼 클릭 (플러그 아이콘)
   - 또는: 메뉴 > Database > New Database Connection
   - 단축키: `Ctrl+N` (Windows), `Cmd+N` (macOS)

2. 데이터베이스 유형 선택
   - MySQL, PostgreSQL, Oracle 등 중 선택
   - 우리 회사에서 사용하는 DB 유형 선택 (관리자에게 문의)

3. "Next" 클릭

### 4.2. 연결 정보 입력

필수 정보 입력:

```
Server Host:  [DB 서버 주소]
Port:         [포트 번호, 예: 3306(MySQL), 5432(PostgreSQL)]
Database:     [데이터베이스 이름]
Username:     [사용자 계정]
Password:     [비밀번호]
```

> ⚠️ **중요**: 연결 정보는 관리자나 DBA에게 문의하세요.

예시 (MySQL):
```
Server Host:  db.company.com
Port:         3306
Database:     hr_database
Username:     hr_user
Password:     ********
```

### 4.3. 연결 테스트

1. "Test Connection" 버튼 클릭
2. 성공 메시지 확인:
   - ✅ "Connected" - 성공!
   - ❌ "Cannot establish connection" - 연결 정보 재확인

### 4.4. 드라이버 다운로드

- 처음 연결 시 JDBC 드라이버 다운로드 메시지 표시
- "Download" 클릭하여 자동 설치
- 인터넷 연결 필요

### 4.5. 연결 완료

1. "Finish" 클릭
2. Database Navigator에 새 연결 표시됨
3. 연결 이름을 더블클릭하여 활성화

## 5. 첫 쿼리 실행해보기

### 5.1. SQL 편집기 열기

1. Database Navigator에서 데이터베이스 연결 선택
2. 상단 도구막대에서 "SQL Editor" 버튼 클릭 (SQL 아이콘)
   - 또는: 단축키 `F3`

### 5.2. 간단한 쿼리 작성

SQL 편집기에 다음 입력:

```sql
SELECT 1 AS test;
```

### 5.3. 쿼리 실행

- 실행 버튼 클릭 (▶ 아이콘)
- 또는: 단축키 `Ctrl+Enter` (Windows), `Cmd+Enter` (macOS)

### 5.4. 결과 확인

하단 Results 패널에 다음과 같이 표시되면 성공:

```
test
----
1
```

## 6. 유용한 단축키

| 기능 | Windows | macOS |
|------|---------|-------|
| SQL 편집기 열기 | `F3` | `F3` |
| 쿼리 실행 | `Ctrl+Enter` | `Cmd+Enter` |
| 새 연결 | `Ctrl+N` | `Cmd+N` |
| 자동완성 | `Ctrl+Space` | `Ctrl+Space` |
| 포맷 정리 | `Ctrl+Shift+F` | `Cmd+Shift+F` |

## 7. 문제 해결

### 연결이 안 될 때

**증상**: "Cannot establish connection" 오류

**해결 방법**:
1. 연결 정보가 정확한지 확인
2. 네트워크 연결 확인 (VPN 필요한 경우 VPN 연결)
3. 방화벽 설정 확인
4. 관리자에게 계정 활성화 여부 확인

### 드라이버 다운로드 실패

**증상**: "Driver download failed" 오류

**해결 방법**:
1. 인터넷 연결 확인
2. DBeaver 재시작 후 재시도
3. 수동 드라이버 다운로드:
   - Database > Driver Manager
   - 해당 드라이버 선택 > "Download" 클릭

### 한글이 깨져 보일 때

**해결 방법**:
1. 연결 설정 편집 (연결 우클릭 > Edit Connection)
2. "Connection settings" > "Driver properties"
3. `characterEncoding` 속성 추가: `utf8` 또는 `utf8mb4`
4. "OK" 클릭 후 재연결

## 8. 다음 단계

축하합니다! 🎉 DBeaver 설치와 데이터베이스 연결을 완료했습니다.

이제 SQL 기초를 배울 준비가 되었습니다.

👉 **[SQL 기초 학습으로 이동](../02-SQL-Basics/)**

---

## 참고 자료

- [DBeaver 공식 문서](https://github.com/dbeaver/dbeaver/wiki)
- [DBeaver 커뮤니티 포럼](https://github.com/dbeaver/dbeaver/discussions)
